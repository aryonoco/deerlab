# SPDX-License-Identifier: 0BSD
# Copyright (c) 2026 Aryan Ameri

# Copy to project root as .env. Must never be committed to version control.
#
# No service principal secrets are required; authentication uses SSH keys
# and SOPS age encryption throughout.
#
# The age private key is bind-mounted from the host at
# ~/.config/sops/age/keys.txt and exposed via SOPS_AGE_KEY_FILE.

# Optional: override the Proxmox host for ad-hoc SSH
# PROXMOX_HOST=

# GITHUB_TOKEN=
